services:
  mongodb:
    image: mongo:7
    container_name: api-mongodb
    restart: unless-stopped

    ports:
      - "27017:27017"

    environment:
      MONGO_URI: ${MONGO_URI}

    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
